export type NicheOption = {
  name: string;
  category: string;
  description: string;
  potential: string;
  competition: string;
};

export type ProblemOption = {
  title: string;
  description: string;
  audience: string;
  severity: string;
  complexity: string;
  example: string;
}; 